run.config:
  engine: nodejs
  engine.config:
    runtime: nodejs-7.0
  extra_packages:
    - nginx

deploy.config:
  extra_steps:
    - npm run build:prod

web.main:
  start:
    nginx: nginx -c /app/nginx.conf
    app: npm run start
